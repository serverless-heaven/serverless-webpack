linters:
  "*.js":
    - "eslint --fix"                        # Run TSLint
    - "prettier --write"                    # Run Prettier
    - "npm test"                            # Run tests
    - "git add"
